
task list_native_project(type: NativeTask) {
    list = "list native-project";
}

task nativeTest(type: NativeTask, dependsOn: list_native_project) {
    test_name = 'tizen sdk cli test for native app'
}

task webTest(type: WebTask) {
    test_name = 'tizen sdk cli test for web app'
    sdk_path = "/home/dkyun77/tizen-sdk"
    profile = "mobile"
}
